import Ember from 'ember';

export default Ember.Controller.extend({
  /*itemselect: null,
  testlist: Ember.String.w('Programming-Fundamentals JAVA Language'),
  actions: {
    change(itemselect) {
      this.set('itemselect', itemselect);
    }
  }*/
});

